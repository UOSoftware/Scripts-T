VERSION=0.56T

[SPEECH spk_generic_chars_players]
ON=*
// SERV.B spk_generic_chars_players: <args> / <name>

IF (<SRC.ACTION> == ID(SKILL_MAGERY)) || (<SRC.ACTION> == SKILL_MAGERY)
	SRC.SYSMESSAGELOC <def.tcolor_red>,<def.empty_cliloc2>, Büyü yaparken konuşamazsınız.
	RETURN 1
ENDIF



[EOF]

VERSION=0.56T

[TYPEDEF ei_generic_items]
ON=@CLICK
REF1=<CRAFTEDBY>
IF (<REF1>) && (<REF1.ISCHAR> && <REF1.ISPLAYER>)
	MESSAGE @,,1 (Üretici: <REF1.NAME>)
	IF (<QUALITY>)
		MESSAGE @,,1 (Kalite: <ISITEMQUALITYNAME <QUALITY>>)
	ENDIF
ENDIF

[eof]
